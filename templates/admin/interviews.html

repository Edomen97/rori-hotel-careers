{% extends "base_admin.html" %}

{% block content %}
<div class="space-y-6">
  <!-- Header -->
  <div class="flex flex-col md:flex-row md:items-center md:justify-between gap-4">
    <div>
      <h1 class="text-2xl font-bold text-slate-100">Interview Management</h1>
      <p class="text-sm text-slate-400">Schedule and manage applicant interviews.</p>
    </div>
    <div>
      <a href="{{ url_for('admin_interview_new') }}" class="inline-flex items-center px-4 py-2 bg-blue-600 hover:bg-blue-700 text-white text-sm font-medium rounded-xl transition-colors shadow-lg shadow-blue-500/20">
        + Schedule New Interview
      </a>
    </div>
  </div>

  <!-- Interviews Table Container -->
  <div class="bg-slate-800/50 backdrop-blur-xl border border-slate-700/50 rounded-2xl overflow-hidden shadow-xl">
    <div class="p-6">
      {% if interviews %}
      <div class="overflow-x-auto">
        <table class="w-full text-left text-sm text-slate-300">
          <thead class="bg-slate-900/50 text-xs uppercase text-slate-400 border-b border-slate-700/50">
            <tr>
              <th class="p-4">Candidate</th>
              <th class="p-4">Position</th>
              <th class="p-4">Date & Time</th>
              <th class="p-4">Type</th>
              <th class="p-4">Status</th>
              <th class="p-4 text-right">Actions</th>
            </tr>
          </thead>
          <tbody class="divide-y divide-slate-700/30">
            {% for interview in interviews %}
            <tr class="hover:bg-slate-700/20 transition-colors">
              <td class="p-4 font-medium text-white">
                {{ interview.application.full_name if interview.application else 'N/A' }}
              </td>
              <td class="p-4">
                {{ interview.application.job.title if interview.application and interview.application.job else 'N/A' }}
              </td>
              <td class="p-4">{{ interview.scheduled_at.strftime('%Y-%m-%d %H:%M') if interview.scheduled_at else '' }}</td>
              <td class="p-4">
                <span class="px-2.5 py-1 text-xs font-semibold rounded-full bg-blue-500/10 text-blue-400 border border-blue-500/20">
                  {{ interview.interview_type }}
                </span>
              </td>
              <td class="p-4">
                <span class="px-2.5 py-1 text-xs font-semibold rounded-full bg-emerald-500/10 text-emerald-400 border border-emerald-500/20">
                  {{ interview.status }}
                </span>
              </td>
              <td class="p-4 text-right">
                <a href="{{ url_for('admin_interview_edit', interview_id=interview.id) }}" class="text-blue-400 hover:text-blue-300 font-medium text-xs bg-blue-500/10 px-3 py-1.5 rounded-lg border border-blue-500/20 transition-colors">
                  Edit
                </a>
              </td>
            </tr>
            {% endfor %}
          </tbody>
        </table>
      </div>
      {% else %}
      <div class="py-12 text-center text-slate-400">
        <svg class="mx-auto h-12 w-12 mb-3 text-slate-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"/>
        </svg>
        <p class="text-base font-medium">No scheduled interviews found</p>
        <p class="text-xs text-slate-500 mt-1">Interviews scheduled for candidates will appear here.</p>
      </div>
      {% endif %}
    </div>
  </div>
</div>
{% endblock %}
