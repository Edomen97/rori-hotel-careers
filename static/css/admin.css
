/* ============================================
   RORI HOTEL CAREERS - LUXURY ADMIN DASHBOARD
   ============================================ */

:root {
  --navy: #0B132B;
  --navy-light: #111C38;
  --gold: #C5A059;
  --gold-light: #E0C078;
  --border-gold: rgba(197, 160, 89, 0.3);
  --transition-smooth: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

/* ===== ADMIN SIDEBAR ===== */
.admin-sidebar {
  background-color: var(--navy);
  min-height: 100vh;
  padding: 1.5rem 0;
  border-right: 1px solid var(--border-gold);
  box-shadow: 4px 0 20px rgba(0, 0, 0, 0.3);
}

.admin-sidebar .nav-link {
  color: rgba(255, 255, 255, 0.7);
  padding: 0.8rem 1.5rem;
  font-weight: 500;
  font-size: 0.92rem;
  display: flex;
  align-items: center;
  gap: 0.75rem;
  border-left: 3px solid transparent;
  transition: var(--transition-smooth);
}

.admin-sidebar .nav-link:hover {
  color: #ffffff;
  background-color: rgba(197, 160, 89, 0.08);
  border-left-color: rgba(197, 160, 89, 0.5);
  padding-left: 1.75rem;
}

.admin-sidebar .nav-link.active {
  color: var(--gold);
  background-color: rgba(197, 160, 89, 0.15);
  border-left-color: var(--gold);
  font-weight: 600;
}

/* ===== MAIN CONTENT AREA ===== */
.admin-main {
  padding: 2rem;
  background-color: var(--navy-light);
  min-height: 100vh;
  color: #ffffff;
}

/* ===== DARK LUXURY STATUS BADGES ===== */
.status-badge {
  display: inline-flex;
  align-items: center;
  gap: 0.4rem;
  padding: 0.35rem 0.85rem;
  border-radius: 50px;
  font-size: 0.75rem;
  font-weight: 600;
  letter-spacing: 0.5px;
  text-transform: uppercase;
  border: 1px solid transparent;
  backdrop-filter: blur(5px);
}

/* Status: NEW */
.status-new {
  background: rgba(13, 110, 253, 0.15);
  color: #6ea8fe;
  border-color: rgba(13, 110, 253, 0.3);
}

/* Status: UNDER REVIEW */
.status-under-review {
  background: rgba(255, 193, 7, 0.15);
  color: #ffda6a;
  border-color: rgba(255, 193, 7, 0.3);
}

/* Status: SHORTLISTED */
.status-shortlisted {
  background: rgba(13, 202, 240, 0.15);
  color: #6edff6;
  border-color: rgba(13, 202, 240, 0.3);
}

/* Status: INTERVIEW */
.status-interview {
  background: rgba(214, 51, 132, 0.15);
  color: #e685b5;
  border-color: rgba(214, 51, 132, 0.3);
}

/* Status: SELECTED */
.status-selected {
  background: rgba(197, 160, 89, 0.2);
  color: var(--gold);
  border-color: var(--border-gold);
}

/* Status: HIRED */
.status-hired {
  background: rgba(25, 135, 84, 0.2);
  color: #75b798;
  border-color: rgba(25, 135, 84, 0.4);
}

/* Status: REJECTED */
.status-rejected {
  background: rgba(220, 53, 69, 0.18);
  color: #ea868f;
  border-color: rgba(220, 53, 69, 0.35);
}


/* ================================================================
   ADMIN DASHBOARD — DARK MODE FIX
   Added to override white backgrounds from Bootstrap / inline styles
   Preserves Rori Hotel navy + gold luxury design
================================================================ */

/* ---- Admin Cards & Table Dark Mode Fix ---- */
.stat-card,
.card,
.bg-white,
.table-responsive {
    background-color: #111c38 !important;
    border: 1px solid rgba(212, 175, 55, 0.2) !important;
    color: #ffffff !important;
}

.stat-card h3,
.stat-card p,
.card h3,
.card p {
    color: #f8fafc !important;
}

table {
    background-color: #111c38 !important;
    color: #e2e8f0 !important;
}

th {
    background-color: #1a2b56 !important;
    color: #d4af37 !important;
    border-bottom: 1px solid rgba(255, 255, 255, 0.1) !important;
}

td {
    color: #ffffff !important;
    background-color: #111c38 !important;
    border-bottom: 1px solid rgba(255, 255, 255, 0.05) !important;
}

.text-muted,
.text-gray-500,
td small {
    color: #94a3b8 !important;
}

tbody tr:hover {
    background-color: #17264a !important;
}


/* ================================================================
   BOOTSTRAP DARK MODE OVERRIDES
   Extra fixes for Bootstrap classes that stay white
================================================================ */

/* ---- Card Header (Bootstrap .card-header default is white) ---- */
.card-header {
    background-color: #17264a !important;
    color: #ffffff !important;
    border-bottom: 1px solid rgba(212, 175, 55, 0.2) !important;
}

.card-header h1,
.card-header h2,
.card-header h3,
.card-header h4,
.card-header h5,
.card-header h6 {
    color: #ffffff !important;
}

.card-header .text-muted,
.card-header small {
    color: #94a3b8 !important;
}

/* ---- Card Body (in case bg-white was applied) ---- */
.card-body {
    background-color: #111c38 !important;
    color: #ffffff !important;
}

/* ---- Card Footer ---- */
.card-footer {
    background-color: #17264a !important;
    border-top: 1px solid rgba(212, 175, 55, 0.2) !important;
    color: #ffffff !important;
}

/* ---- Bootstrap .table with no background ---- */
.table {
    --bs-table-bg: transparent !important;
    --bs-table-color: #e2e8f0 !important;
    --bs-table-border-color: rgba(255, 255, 255, 0.05) !important;
    --bs-table-hover-bg: #17264a !important;
    --bs-table-hover-color: #ffffff !important;
    color: #e2e8f0 !important;
    background-color: transparent !important;
}

/* ---- Bootstrap .table-light variants ---- */
.table-light,
.table > :not(caption) > * > * {
    background-color: #111c38 !important;
    color: #e2e8f0 !important;
}

.table-light th,
.table-light td {
    background-color: #111c38 !important;
    color: #e2e8f0 !important;
}

/* ---- Bootstrap table header ---- */
.table thead th {
    background-color: #1a2b56 !important;
    color: #d4af37 !important;
    border-color: rgba(255, 255, 255, 0.1) !important;
}

/* ---- Bootstrap hover rows ---- */
.table-hover > tbody > tr:hover > * {
    background-color: #17264a !important;
    color: #ffffff !important;
}

/* ---- Striped rows ---- */
.table-striped > tbody > tr:nth-of-type(odd) > * {
    background-color: #0f1a33 !important;
    color: #e2e8f0 !important;
}

.table-striped > tbody > tr:nth-of-type(even) > * {
    background-color: #111c38 !important;
    color: #e2e8f0 !important;
}

/* ---- Bootstrap badges (dark friendly) ---- */
.badge {
    color: #ffffff !important;
}

.badge.bg-secondary {
    background-color: #475569 !important;
    color: #ffffff !important;
}

.badge.bg-light {
    background-color: #1e293b !important;
    color: #e2e8f0 !important;
}

.badge.bg-warning {
    background-color: #d4af37 !important;
    color: #0b132b !important;
}

.badge.bg-success {
    background-color: #16a34a !important;
    color: #ffffff !important;
}

.badge.bg-danger {
    background-color: #dc2626 !important;
    color: #ffffff !important;
}

.badge.bg-info {
    background-color: #0891b2 !important;
    color: #ffffff !important;
}

/* ---- Bootstrap alert inside cards ---- */
.card .alert {
    background-color: #17264a !important;
    border-color: rgba(212, 175, 55, 0.3) !important;
    color: #e2e8f0 !important;
}

/* ---- Bootstrap table border-less tables ---- */
.table-borderless > :not(caption) > * > * {
    border-bottom-width: 0 !important;
}

/* ---- Table wrapper ---- */
.table-responsive {
    background-color: #111c38 !important;
}

/* ---- Modal content (in case it's used) ---- */
.modal-content {
    background-color: #111c38 !important;
    color: #ffffff !important;
    border: 1px solid rgba(212, 175, 55, 0.3) !important;
}

.modal-header,
.modal-footer {
    background-color: #17264a !important;
    color: #ffffff !important;
    border-color: rgba(212, 175, 55, 0.2) !important;
}

.modal-body {
    background-color: #111c38 !important;
    color: #e2e8f0 !important;
}

/* ---- Close button visibility on dark ---- */
.btn-close {
    filter: invert(1) brightness(2) !important;
    opacity: 0.7 !important;
}

.btn-close:hover {
    opacity: 1 !important;
}

/* ---- Bootstrap list-group in admin (if any) ---- */
.list-group-item {
    background-color: #111c38 !important;
    color: #e2e8f0 !important;
    border-color: rgba(255, 255, 255, 0.05) !important;
}

/* ---- Pagination (if any) ---- */
.page-link {
    background-color: #111c38 !important;
    color: #d4af37 !important;
    border-color: rgba(212, 175, 55, 0.3) !important;
}

.page-link:hover {
    background-color: #17264a !important;
    color: #ffffff !important;
}

.page-item.active .page-link {
    background-color: #d4af37 !important;
    color: #0b132b !important;
    border-color: #d4af37 !important;
}

/* ---- Forms in admin (search/filters) ---- */
.form-control,
.form-select {
    background-color: #0f1a33 !important;
    color: #ffffff !important;
    border: 1px solid rgba(212, 175, 55, 0.3) !important;
}

.form-control::placeholder {
    color: #64748b !important;
}

.form-control:focus,
.form-select:focus {
    background-color: #0f1a33 !important;
    color: #ffffff !important;
    border-color: #d4af37 !important;
    box-shadow: 0 0 0 0.2rem rgba(212, 175, 55, 0.25) !important;
}

/* ---- Dark select dropdown ---- */
.form-select option {
    background-color: #0b132b !important;
    color: #ffffff !important;
}

/* ---- Responsive — ensure mobile doesn't break ---- */
@media (max-width: 767px) {
    .table-responsive {
        background-color: #111c38 !important;
    }

    .card,
    .stat-card {
        background-color: #111c38 !important;
    }

    .card-header,
    .card-body,
    .card-footer {
        background-color: #111c38 !important;
    }

    th {
        font-size: 0.75rem !important;
        padding: 0.6rem 0.5rem !important;
    }

    td {
        font-size: 0.8rem !important;
        padding: 0.6rem 0.5rem !important;
    }
}/* ================================================================
   SUMMARY CARDS DARK MODE FIX
   (Interview Management + Dashboard + Candidates pages)
================================================================ */

/* ---- Summary Card base ---- */
.summary-card {
    background-color: #111c38 !important;
    border: 1px solid rgba(197, 160, 89, 0.25) !important;
    color: #ffffff !important;
    border-radius: 14px;
    padding: 1.1rem;
    display: flex;
    align-items: center;
    gap: 1rem;
    box-shadow: 0 6px 20px rgba(0, 0, 0, 0.3) !important;
    transition: all 0.3s ease;
}

.summary-card:hover {
    border-color: rgba(197, 160, 89, 0.5) !important;
    transform: translateY(-3px);
    box-shadow: 0 10px 28px rgba(0, 0, 0, 0.4) !important;
}

/* ---- Icon inside summary card ---- */
.summary-icon {
    background-color: rgba(197, 160, 89, 0.15) !important;
    color: #C5A059 !important;
    width: 42px;
    height: 42px;
    flex-shrink: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 11px;
    font-size: 1.15rem;
}

/* ---- Label (small text) ---- */
.summary-label {
    color: #94a3b8 !important;
    font-size: 0.72rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    margin-bottom: 0.2rem;
}

/* ---- Value (big number) — CRITICAL FIX ---- */
.summary-value {
    color: #ffffff !important;
    font-size: 1.55rem;
    font-weight: 800;
    line-height: 1.1;
    text-shadow: 0 1px 2px rgba(0, 0, 0, 0.4);
}

/* ---- Fallback for other card variants ---- */
.interview-card {
    background-color: #111c38 !important;
    border: 1px solid rgba(197, 160, 89, 0.2) !important;
}

.interview-card .card-header {
    background-color: #17264a !important;
    border-bottom: 1px solid rgba(197, 160, 89, 0.15) !important;
}

.interview-card .card-header h5 {
    color: #ffffff !important;
}

.interview-card .card-header small,
.interview-card .card-header .text-muted {
    color: #94a3b8 !important;
}

/* ================================================================
   INTERVIEWS TABLE — DARK MODE
================================================================ */

.interview-card .table,
.interview-card .table tbody,
.interview-card .table thead,
table.table {
    background-color: #111c38 !important;
    color: #e2e8f0 !important;
}

.interview-card .table thead th,
table.table thead th {
    background-color: #1a2b56 !important;
    color: #d4af37 !important;
    border-bottom: 1px solid rgba(255, 255, 255, 0.1) !important;
    font-weight: 700;
}

.interview-card .table tbody td,
table.table tbody td {
    background-color: #111c38 !important;
    color: #ffffff !important;
    border-bottom: 1px solid rgba(255, 255, 255, 0.05) !important;
}

.interview-card .table tbody tr:hover,
table.table tbody tr:hover {
    background-color: #17264a !important;
}

.interview-card .table tbody tr:hover td,
table.table tbody tr:hover td {
    background-color: #17264a !important;
}

/* Table inner text elements */
.interview-card .table .candidate-name,
.interview-card .table .job-title,
.interview-card .table .interview-id {
    color: #ffffff !important;
}

.interview-card .table .candidate-email,
.interview-card .table .text-muted {
    color: #94a3b8 !important;
}

/* ================================================================
   GENERIC .card FIX — covers most interview/dashboard pages
================================================================ */

.card {
    background-color: #111c38 !important;
    border: 1px solid rgba(197, 160, 89, 0.2) !important;
    color: #ffffff !important;
}

.card-body {
    background-color: #111c38 !important;
    color: #ffffff !important;
}

.card-body h1,
.card-body h2,
.card-body h3,
.card-body h4,
.card-body h5,
.card-body h6 {
    color: #ffffff !important;
}

.card-body p,
.card-body span,
.card-body label {
    color: #e2e8f0;
}

.card-body .text-muted,
.card-body small {
    color: #94a3b8 !important;
}

/* ================================================================
   RESPONSIVE — make sure mobile works too
================================================================ */

@media (max-width: 767px) {
    .summary-card {
        padding: 0.85rem !important;
        border-radius: 12px !important;
    }
    
    .summary-value {
        font-size: 1.3rem !important;
    }
    
    .summary-label {
        font-size: 0.65rem !important;
    }
    
    .summary-icon {
        width: 36px !important;
        height: 36px !important;
        font-size: 1rem !important;
    }
}